# Example environment variables for local development and CI
VITE_SUPABASE_URL=https://xyz.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=anon-xxx
# Optional: A direct backend API URL (use this if you host a serverless API that talks to MongoDB Atlas)
VITE_API_URL=https://api.example.com

# MONGODB_URI should never be stored in repo; set it in the service that hosts your API (e.g. Vercel/Render/MongoDB Realm/Supabase Secrets)
# MONGODB_URI=mongodb+srv://user:password@cluster0.mongodb.net/dashboard?retryWrites=true&w=majority
